// The public URL of the KARINEX app backend that exposes /api/deliveries.
// Set this to your SHOPIFY_APP_URL before running `shopify app deploy`.
// (UI extensions have no runtime env vars, so the backend URL is baked in.)
export const APP_URL = "https://digtale-production.up.railway.app";
